{
  "name": "sense-v1",
  "version": "1.0.0",
  "description": "Sense V1 Core",
  "author": "Federico M. Alconada Verzini",
  "license": "ISC",
  "scripts": {
    "build": "dapp build",
    "debug": "dapp test",
    "test": "dapp test -m \".t.sol\"",
    "testcov": "dapp test --coverage",
    "lint": "npx solhint -f table src/**/*.sol",
    "fix": "prettier --write src/**/*.sol && yarn lint --fix",
    "test-mainnet": "dapp test --rpc -m -m \".tm.sol\""
  },
  "devDependencies": {
    "prettier": "^2.3.2",
    "prettier-plugin-solidity": "^1.0.0-beta.17",
    "solhint": "^3.3.6",
    "solhint-plugin-prettier": "^0.0.5"
  }
}
